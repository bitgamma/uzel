/*#discoverDevices(cb)
#pair(device, data, cb)
#unpair(device, data, cb)
#send(device, data, cb)*/

var tlv = require('tlv');

exports.start = function() {
  
}

exports.send = function(device, tlv, cb) {
  console.log("Send invoked");
}